// MIXINS
skewed(x, h) {
  position: relative
  &:after {
    content: ''
    display: block
    width: 100%
    height: h 
    position: absolute
    transform: skewY(x) 
    { block }
  }
}

center(x) {
  width: 90%
  max-width: max-w
  margin: x auto
}

on(x) {
  @media x { block }
}
